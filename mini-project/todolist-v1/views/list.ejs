<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todolist App</title>
    <style>
        header .title {
            text-align: center;
            background-color: aquamarine;
        }
        .title h1 {
            font-size: 4rem;
        }
        .title p {
            font-size: 2rem;
        }
        .list-item, .input-list {
            text-align: center;
            font-size: 2rem;
        }
    </style>
</head>
<body>
    <header>
        <div class="title">
            <h1>Todolist Simple</h1>
            <p><%= dayFormatted %></p>
        </div>
    </header>

    <main>
        <div class="list-item">
            <ul>
                <% for(let i = 0; i < listItem.length; i++) {%>
                    <li><%= listItem[i] %></li>
                <% } %>
            </ul>
        </div>
        <div class="input-list">
            <form method="post">
                <label for="newlist">Input new list :</label>
                <input type="text" name="newList" id="newlist">
                <button type="submit">Add</button>
            </form>
        </div>
    </main>
</body>
</html>